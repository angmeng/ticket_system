<%= simple_form_for(@agent_group, :html => { :class => 'form-horizontal' }) do |f| %>
  <%= f.input :code %>
  <%= f.input :name %>
  <%= f.input :active %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                agent_groups_path, :class => 'btn' %>
  </div>
<% end %>
