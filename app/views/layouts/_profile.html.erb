  <div class="group-menu nav-collapse"> 
    <ul class="nav pull-right">
        <li class="divider-vertical"></li>
        <li class="dropdown">
            <a data-toggle="dropdown" href="#">Welcome, <%= current_user.username %> <b class="caret"></b></a>
           <ul class="dropdown-menu">
                <li>

                    <div class="modal-header">

                        <h3><%= current_user.fullname  %></h3>
                    </div>
                    <div class="modal-body">
                        <div class="row">
                            <div class="span1"><img src="/images/avatar/photo.png" alt="avatar" /></div>
                            <div class="span3 pull-right">
                                <h5><%= current_user.email %></h5>
                                <!-- <a href="#" class="link-modal" >Account</a>  <a href="#" class="link-modal" >Settings-Privacy</a> -->

                            </div>
                        </div>
                    </div>
                    <div class="modal-footer">
                        <a href="/users/<%= current_user.id %>/profile" class="btn btn-info pull-left">Profile</a>
                          <% if is_manager? || is_staff? %>
                             <%= link_to 'Sign out', destroy_staff_session_path, :method => :delete, :class => "btn btn-info" %>
                          <% elsif is_agent? %>
                             <%= link_to 'Sign out', destroy_agent_session_path, :method => :delete, :class => "btn btn-info" %>
                          <% elsif is_user? %>
                             <%= link_to 'Sign out', destroy_user_session_path, :method => :delete, :class => "btn btn-info" %>
                          <% end %>
                        <!-- <a class="btn btn-info" href="login.html">Logout</a> -->
                    </div>

                </li>
            </ul>

        </li>
    </ul>

    <form action="#" class="navbar-search pull-right">
        <input type="text" placeholder="Search" class="search-query span2" >
    </form>
</div>