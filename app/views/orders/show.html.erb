<%= render "order_detail" %>

<%= link_to "Printable Version", preview_printable_order_path(@order, :format => "pdf"), :class => 'btn', :target => "_blank"  %>
<%= link_to t('.back', :default => t("helpers.links.back")), orders_path, :class => 'btn'  %>